{
  "hooks": {
    "SessionStart": [
      {
        "matcher": "",
        "hooks": [
          {
            "type": "command",
            "command": "cd trading-algo && npm install --prefer-offline --no-audit 2>/dev/null || npm install && npm run seed-data 2>/dev/null || true"
          }
        ]
      }
    ]
  }
}
